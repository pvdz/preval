# Preval test case

# identity.md

> base > identity
>
> A function returning its arg then called with a string

## Input

`````js filename=intro
f('x')

function f(a) {
  return a
}
`````

## Output

`````js filename=intro
f('x');
function f(a) {
  return a;
}
`````
