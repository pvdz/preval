# Preval test case

# call.md

> normalize > new > call
>
> Make sure empty array works

## Input

`````js filename=intro
$([]);
`````

## Normalized

`````js filename=intro
$([]);
`````

## Uniformed

`````js filename=intro
x([]);
`````

## Output

`````js filename=intro
$([]);
`````
