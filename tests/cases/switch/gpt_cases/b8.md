# Preval test case

# b8.md

> Switch > Gpt cases > B8
>
> conditional return

GPT:
Why it’s troublesome:
- this is no longer just “entry index + monotone suffix execution”
- whether later code runs depends on runtime control flow inside a case body

This is the first one I’d expect to break a pure range-guard lowering.

Preval: lollollol

## Input

`````js filename=intro
function f(x) {
  switch (x) {
    case $(10):
      $(1);
      if ($(9)) return $(99);
      $(2);
    default:
      $('d');
    case $(20):
      $(3);
  }
}
$(f); // prevent DCE
`````


## Settled


`````js filename=intro
const f /*:(unknown)=>unknown*/ = function ($$0) {
  const x /*:unknown*/ = $$0;
  debugger;
  let tmpSwitchCaseToStart /*:number*/ /*ternaryConst*/ = 1;
  const tmpBinLhs /*:unknown*/ = $(10);
  const tmpIfTest /*:boolean*/ = tmpBinLhs === x;
  if (tmpIfTest) {
    tmpSwitchCaseToStart = 0;
  } else {
    const tmpBinLhs$1 /*:unknown*/ = $(20);
    const tmpIfTest$1 /*:boolean*/ = tmpBinLhs$1 === x;
    if (tmpIfTest$1) {
      tmpSwitchCaseToStart = 2;
    } else {
    }
  }
  const tmpIfTest$3 /*:boolean*/ = tmpSwitchCaseToStart <= 0;
  if (tmpIfTest$3) {
    $(1);
    const tmpIfTest$5 /*:unknown*/ = $(9);
    if (tmpIfTest$5) {
      const tmpReturnArg /*:unknown*/ = $(99);
      return tmpReturnArg;
    } else {
      $(2);
    }
  } else {
  }
  const tmpIfTest$7 /*:boolean*/ = tmpSwitchCaseToStart <= 1;
  if (tmpIfTest$7) {
    $(`d`);
    $(3);
    return undefined;
  } else {
    $(3);
    return undefined;
  }
};
$(f);
`````


## Denormalized
(This ought to be the final result)

`````js filename=intro
$(function (x) {
  let tmpSwitchCaseToStart = 1;
  if ($(10) === x) {
    tmpSwitchCaseToStart = 0;
  } else {
    if ($(20) === x) {
      tmpSwitchCaseToStart = 2;
    }
  }
  if (tmpSwitchCaseToStart <= 0) {
    $(1);
    if ($(9)) {
      const tmpReturnArg = $(99);
      return tmpReturnArg;
    } else {
      $(2);
    }
  }
  if (tmpSwitchCaseToStart <= 1) {
    $(`d`);
    $(3);
  } else {
    $(3);
  }
});
`````


## PST Settled
With rename=true

`````js filename=intro
const a = function($$0 ) {
  const b = $$0;
  debugger;
  let c = 1;
  const d = $( 10 );
  const e = d === b;
  if (e) {
    c = 0;
  }
  else {
    const f = $( 20 );
    const g = f === b;
    if (g) {
      c = 2;
    }
  }
  const h = c <= 0;
  if (h) {
    $( 1 );
    const i = $( 9 );
    if (i) {
      const j = $( 99 );
      return j;
    }
    else {
      $( 2 );
    }
  }
  const k = c <= 1;
  if (k) {
    $( "d" );
    $( 3 );
    return undefined;
  }
  else {
    $( 3 );
    return undefined;
  }
};
$( a );
`````


## Normalized
(This is what phase1 received the first time)

`````js filename=intro
let f = function ($$0) {
  let x = $$0;
  debugger;
  let tmpSwitchValue = x;
  let tmpSwitchCaseToStart = 1;
  const tmpBinLhs = $(10);
  const tmpIfTest = tmpBinLhs === tmpSwitchValue;
  if (tmpIfTest) {
    tmpSwitchCaseToStart = 0;
  } else {
    const tmpBinLhs$1 = $(20);
    const tmpIfTest$1 = tmpBinLhs$1 === tmpSwitchValue;
    if (tmpIfTest$1) {
      tmpSwitchCaseToStart = 2;
    } else {
    }
  }
  const tmpIfTest$3 = tmpSwitchCaseToStart <= 0;
  if (tmpIfTest$3) {
    $(1);
    const tmpIfTest$5 = $(9);
    if (tmpIfTest$5) {
      const tmpReturnArg = $(99);
      return tmpReturnArg;
    } else {
      $(2);
    }
  } else {
  }
  const tmpIfTest$7 = tmpSwitchCaseToStart <= 1;
  if (tmpIfTest$7) {
    $(`d`);
  } else {
  }
  const tmpIfTest$9 = tmpSwitchCaseToStart <= 2;
  if (tmpIfTest$9) {
    $(3);
    return undefined;
  } else {
    return undefined;
  }
};
$(f);
`````


## Todos triggered


None


## Globals


None


## Runtime Outcome


Should call `$` with:
 - 1: '<function>'
 - eval returned: undefined

Pre normalization calls: Same

Normalized calls: Same

Post settled calls: Same

Denormalized calls: Same
