# Preval test case

# c1.md

> Switch > Gpt cases > C1
>
> loops!

## Input

`````js filename=intro
while ($(0)) {
  switch (x) {
    case $(10):
      $(1);
      continue;
    case $(20):
      $(2);
      break;
    default:
      $('d');
  }
  $(3);
}
`````


## Settled


`````js filename=intro
const tmpIfTest /*:unknown*/ = $(0);
if (tmpIfTest) {
  const tmpSwitchDisc /*:unknown*/ = x;
  const tmpBinBothRhs /*:unknown*/ = $(10);
  const tmpIfTest$1 /*:boolean*/ = tmpSwitchDisc === tmpBinBothRhs;
  if (tmpIfTest$1) {
    $(1);
  } else {
    const tmpBinBothRhs$1 /*:unknown*/ = $(20);
    const tmpIfTest$3 /*:boolean*/ = tmpSwitchDisc === tmpBinBothRhs$1;
    if (tmpIfTest$3) {
      $(2);
      $(3);
    } else {
      $(`d`);
      $(3);
    }
  }
  while ($LOOP_UNROLLS_LEFT_10) {
    const tmpIfTest$2 /*:unknown*/ = $(0);
    if (tmpIfTest$2) {
      const tmpSwitchDisc$1 /*:unknown*/ = x;
      const tmpBinBothRhs$2 /*:unknown*/ = $(10);
      const tmpIfTest$4 /*:boolean*/ = tmpSwitchDisc$1 === tmpBinBothRhs$2;
      if (tmpIfTest$4) {
        $(1);
      } else {
        const tmpBinBothRhs$4 /*:unknown*/ = $(20);
        const tmpIfTest$6 /*:boolean*/ = tmpSwitchDisc$1 === tmpBinBothRhs$4;
        if (tmpIfTest$6) {
          $(2);
          $(3);
        } else {
          $(`d`);
          $(3);
        }
      }
    } else {
      break;
    }
  }
} else {
}
`````


## Denormalized
(This ought to be the final result)

`````js filename=intro
if ($(0)) {
  const tmpSwitchDisc = x;
  if (tmpSwitchDisc === $(10)) {
    $(1);
  } else {
    if (tmpSwitchDisc === $(20)) {
      $(2);
      $(3);
    } else {
      $(`d`);
      $(3);
    }
  }
  while (true) {
    if ($(0)) {
      const tmpSwitchDisc$1 = x;
      if (tmpSwitchDisc$1 === $(10)) {
        $(1);
      } else {
        if (tmpSwitchDisc$1 === $(20)) {
          $(2);
          $(3);
        } else {
          $(`d`);
          $(3);
        }
      }
    } else {
      break;
    }
  }
}
`````


## PST Settled
With rename=true

`````js filename=intro
const a = $( 0 );
if (a) {
  const b = x;
  const c = $( 10 );
  const d = b === c;
  if (d) {
    $( 1 );
  }
  else {
    const e = $( 20 );
    const f = b === e;
    if (f) {
      $( 2 );
      $( 3 );
    }
    else {
      $( "d" );
      $( 3 );
    }
  }
  while ($LOOP_UNROLLS_LEFT_10) {
    const g = $( 0 );
    if (g) {
      const h = x;
      const i = $( 10 );
      const j = h === i;
      if (j) {
        $( 1 );
      }
      else {
        const k = $( 20 );
        const l = h === k;
        if (l) {
          $( 2 );
          $( 3 );
        }
        else {
          $( "d" );
          $( 3 );
        }
      }
    }
    else {
      break;
    }
  }
}
`````


## Normalized
(This is what phase1 received the first time)

`````js filename=intro
while (true) {
  const tmpIfTest = $(0);
  if (tmpIfTest) {
    $continue: {
      tmpSwitchBreak: {
        const tmpSwitchDisc = x;
        const tmpBinBothLhs = tmpSwitchDisc;
        const tmpBinBothRhs = $(10);
        const tmpIfTest$1 = tmpBinBothLhs === tmpBinBothRhs;
        if (tmpIfTest$1) {
          $(1);
          break $continue;
        } else {
          const tmpBinBothLhs$1 = tmpSwitchDisc;
          const tmpBinBothRhs$1 = $(20);
          const tmpIfTest$3 = tmpBinBothLhs$1 === tmpBinBothRhs$1;
          if (tmpIfTest$3) {
            $(2);
            break tmpSwitchBreak;
          } else {
            $(`d`);
          }
        }
      }
      $(3);
    }
  } else {
    break;
  }
}
`````


## Todos triggered


None


## Globals


BAD@! Found 1 implicit global bindings:

x


## Runtime Outcome


Should call `$` with:
 - 1: 0
 - eval returned: undefined

Pre normalization calls: Same

Normalized calls: Same

Post settled calls: Same

Denormalized calls: Same
