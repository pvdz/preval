# Preval test case

# c2.md

> Switch > Gpt cases > C2
>
> labeled break out of outer construct

## Input

`````js filename=intro
outer: for (;;) {
  switch (x) {
    case $(10):
      $(1);
      break outer;
    default:
      $('d');
  }
  $(2);
}
`````


## Settled


`````js filename=intro
const tmpBinBothLhs /*:unknown*/ = x;
const tmpBinBothRhs /*:unknown*/ = $(10);
const tmpIfTest /*:boolean*/ = tmpBinBothLhs === tmpBinBothRhs;
if (tmpIfTest) {
  $(1);
} else {
  while ($LOOP_UNROLLS_LEFT_10) {
    $(`d`);
    $(2);
    const tmpBinBothLhs$1 /*:unknown*/ = x;
    const tmpBinBothRhs$1 /*:unknown*/ = $(10);
    const tmpIfTest$1 /*:boolean*/ = tmpBinBothLhs$1 === tmpBinBothRhs$1;
    if (tmpIfTest$1) {
      $(1);
      break;
    } else {
    }
  }
}
`````


## Denormalized
(This ought to be the final result)

`````js filename=intro
if (x === $(10)) {
  $(1);
} else {
  while (true) {
    $(`d`);
    $(2);
    if (x === $(10)) {
      $(1);
      break;
    }
  }
}
`````


## PST Settled
With rename=true

`````js filename=intro
const a = x;
const b = $( 10 );
const c = a === b;
if (c) {
  $( 1 );
}
else {
  while ($LOOP_UNROLLS_LEFT_10) {
    $( "d" );
    $( 2 );
    const d = x;
    const e = $( 10 );
    const f = d === e;
    if (f) {
      $( 1 );
      break;
    }
  }
}
`````


## Normalized
(This is what phase1 received the first time)

`````js filename=intro
while (true) {
  const tmpSwitchDisc = x;
  const tmpBinBothLhs = tmpSwitchDisc;
  const tmpBinBothRhs = $(10);
  const tmpIfTest = tmpBinBothLhs === tmpBinBothRhs;
  if (tmpIfTest) {
    $(1);
    break;
  } else {
    $(`d`);
    $(2);
  }
}
`````


## Todos triggered


None


## Globals


BAD@! Found 1 implicit global bindings:

x


## Runtime Outcome


Should call `$` with:
 - eval returned: ('<crash[ <ref> is not defined ]>')

Pre normalization calls: Same

Normalized calls: Same

Post settled calls: Same

Denormalized calls: Same
