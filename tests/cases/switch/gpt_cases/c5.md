# Preval test case

# c5.md

> Switch > Gpt cases > C5
>
> inner witch with break

## Input

`````js filename=intro
switch (x) {
  case $(10):
    switch ($(20)) {
      case $(30): break;
      default: $(40);
    }
    $(1);
    break;
  default:
    $('d');
}
`````


## Settled


`````js filename=intro
const tmpBinBothLhs /*:unknown*/ = x;
const tmpBinBothRhs /*:unknown*/ = $(10);
const tmpIfTest /*:boolean*/ = tmpBinBothLhs === tmpBinBothRhs;
if (tmpIfTest) {
  const tmpBinBothLhs$1 /*:unknown*/ = $(20);
  const tmpBinBothRhs$1 /*:unknown*/ = $(30);
  const tmpIfTest$1 /*:boolean*/ = tmpBinBothLhs$1 === tmpBinBothRhs$1;
  if (tmpIfTest$1) {
    $(1);
  } else {
    $(40);
    $(1);
  }
} else {
  $(`d`);
}
`````


## Denormalized
(This ought to be the final result)

`````js filename=intro
if (x === $(10)) {
  if ($(20) === $(30)) {
    $(1);
  } else {
    $(40);
    $(1);
  }
} else {
  $(`d`);
}
`````


## PST Settled
With rename=true

`````js filename=intro
const a = x;
const b = $( 10 );
const c = a === b;
if (c) {
  const d = $( 20 );
  const e = $( 30 );
  const f = d === e;
  if (f) {
    $( 1 );
  }
  else {
    $( 40 );
    $( 1 );
  }
}
else {
  $( "d" );
}
`````


## Normalized
(This is what phase1 received the first time)

`````js filename=intro
tmpSwitchBreak: {
  const tmpSwitchDisc = x;
  const tmpBinBothLhs = tmpSwitchDisc;
  const tmpBinBothRhs = $(10);
  const tmpIfTest = tmpBinBothLhs === tmpBinBothRhs;
  if (tmpIfTest) {
    tmpSwitchBreak$1: {
      const tmpSwitchDisc$1 = $(20);
      const tmpBinBothLhs$1 = tmpSwitchDisc$1;
      const tmpBinBothRhs$1 = $(30);
      const tmpIfTest$1 = tmpBinBothLhs$1 === tmpBinBothRhs$1;
      if (tmpIfTest$1) {
        break tmpSwitchBreak$1;
      } else {
        $(40);
      }
    }
    $(1);
    break tmpSwitchBreak;
  } else {
    $(`d`);
  }
}
`````


## Todos triggered


None


## Globals


BAD@! Found 1 implicit global bindings:

x


## Runtime Outcome


Should call `$` with:
 - eval returned: ('<crash[ <ref> is not defined ]>')

Pre normalization calls: Same

Normalized calls: Same

Post settled calls: Same

Denormalized calls: Same
