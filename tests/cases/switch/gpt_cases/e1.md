# Preval test case

# e1.md

> Switch > Gpt cases > E1
>
> switch lex var case scoping

## Input

`````js filename=intro
switch (3) {
  case $(0):
    const a = $(1);
    var v = $(2);
  default:
    $(a);
    $(v);
    break;

  case $(3):
    $(typeof a);
    $(v);
    break;
}
`````


## Settled


`````js filename=intro
const tmpBinLhs /*:unknown*/ = $(0);
let tmpSwitchCaseToStart /*:number*/ /*ternaryConst*/ = 1;
let a /*:unknown*/ /*ternaryConst*/ = undefined;
let v /*:unknown*/ /*ternaryConst*/ = undefined;
const tmpIfTest /*:boolean*/ = tmpBinLhs === 3;
if (tmpIfTest) {
  tmpSwitchCaseToStart = 0;
} else {
  const tmpBinLhs$1 /*:unknown*/ = $(3);
  const tmpIfTest$1 /*:boolean*/ = tmpBinLhs$1 === 3;
  if (tmpIfTest$1) {
    tmpSwitchCaseToStart = 2;
  } else {
  }
}
const tmpIfTest$3 /*:boolean*/ = tmpSwitchCaseToStart <= 0;
if (tmpIfTest$3) {
  a = $(1);
  v = $(2);
} else {
}
const tmpIfTest$5 /*:boolean*/ = tmpSwitchCaseToStart <= 1;
if (tmpIfTest$5) {
  $(a);
  $(v);
} else {
  const tmpCalleeParam /*:string*/ /*truthy*/ = typeof a;
  $(tmpCalleeParam);
  $(v);
}
`````


## Denormalized
(This ought to be the final result)

`````js filename=intro
const tmpBinLhs = $(0);
let tmpSwitchCaseToStart = 1;
let a = undefined;
let v = undefined;
if (tmpBinLhs === 3) {
  tmpSwitchCaseToStart = 0;
} else {
  if ($(3) === 3) {
    tmpSwitchCaseToStart = 2;
  }
}
if (tmpSwitchCaseToStart <= 0) {
  a = $(1);
  v = $(2);
}
if (tmpSwitchCaseToStart <= 1) {
  $(a);
  $(v);
} else {
  $(typeof a);
  $(v);
}
`````


## PST Settled
With rename=true

`````js filename=intro
const a = $( 0 );
let b = 1;
let c = undefined;
let d = undefined;
const e = a === 3;
if (e) {
  b = 0;
}
else {
  const f = $( 3 );
  const g = f === 3;
  if (g) {
    b = 2;
  }
}
const h = b <= 0;
if (h) {
  c = $( 1 );
  d = $( 2 );
}
const i = b <= 1;
if (i) {
  $( c );
  $( d );
}
else {
  const j = typeof c;
  $( j );
  $( d );
}
`````


## Normalized
(This is what phase1 received the first time)

`````js filename=intro
let v = undefined;
let a = undefined;
let tmpSwitchValue = 3;
let tmpSwitchCaseToStart = 1;
const tmpBinLhs = $(0);
const tmpIfTest = tmpBinLhs === tmpSwitchValue;
tmpSwitchBreak: {
  if (tmpIfTest) {
    tmpSwitchCaseToStart = 0;
  } else {
    const tmpBinLhs$1 = $(3);
    const tmpIfTest$1 = tmpBinLhs$1 === tmpSwitchValue;
    if (tmpIfTest$1) {
      tmpSwitchCaseToStart = 2;
    } else {
    }
  }
  const tmpIfTest$3 = tmpSwitchCaseToStart <= 0;
  if (tmpIfTest$3) {
    a = $(1);
    v = $(2);
  } else {
  }
  const tmpIfTest$5 = tmpSwitchCaseToStart <= 1;
  if (tmpIfTest$5) {
    $(a);
    $(v);
    break tmpSwitchBreak;
  } else {
    const tmpIfTest$7 = tmpSwitchCaseToStart <= 2;
    if (tmpIfTest$7) {
      let tmpCalleeParam = typeof a;
      $(tmpCalleeParam);
      $(v);
      break tmpSwitchBreak;
    } else {
    }
  }
}
`````


## Todos triggered


None


## Globals


None


## Runtime Outcome


Should call `$` with:
 - 1: 0
 - 2: 3
 - eval returned: ("<crash[ Cannot access '<ref>' before initialization ]>")

Pre normalization calls: Same

Normalized calls: BAD!?
 -  1: 0
 -  2: 3
 - !3: 'undefined'
 - !4: undefined
 - !eval returned: undefined

Post settled calls: BAD!!
 -  1: 0
 -  2: 3
 - !3: 'undefined'
 - !4: undefined
 - !eval returned: undefined

Denormalized calls: BAD!!
 -  1: 0
 -  2: 3
 - !3: 'undefined'
 - !4: undefined
 - !eval returned: undefined
